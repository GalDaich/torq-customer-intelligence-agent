# Sample report — pending a real local run

This file intentionally contains no fixture or model-simulated report.

A valid submission artifact must come from a real local run using Tavily, Firecrawl, OpenAI, and LangSmith. It must preserve the rendered report structure, actual public source URLs, evidence-backed claims, the real `researchId`, and the matching LangSmith trace.

Replace this notice only after the live acceptance run described in `README.md` passes. Manufacturing a sample now would misrepresent both its provenance and the product's verified behavior.
